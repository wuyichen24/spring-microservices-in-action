spring:
  application:
    name: authenticationservice   # Don't change to other name, otherwise it can not get the configuration from config server
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true