spring:
  application:
    name: organizationservice
  profiles:
    active:
      default
  zipkin:
     baseUrl: http://localhost:9411/                # Send tracing information to Zipkin server
  cloud:
    config:
      enabled: true