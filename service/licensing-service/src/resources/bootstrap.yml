spring:
  application:
    name: licensingservice        # name of service
  profiles:
    active:
      default                     # envrionment (default, dev, prod)
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888  # location of Spring Cloud Config server